apiVersion: v1
kind: Service
metadata:
  name: runbook-expert-alpine
  namespace: default 
  labels:
    app: runbook-expert-alpine-app
spec:
  selector:
    app: runbook-expert-alpine-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
