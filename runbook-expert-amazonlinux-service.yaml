apiVersion: v1
kind: Service
metadata:
  name: runbook-expert-amazonlinux
  namespace: default 
  labels:
    app: runbook-expert-amazonlinux-app
spec:
  selector:
    app: runbook-expert-amazonlinux-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
